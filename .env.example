# Hue API registration's username (application key).
HUE_USERNAME=
# Optional clientkey from registration; not needed for ambient REST control.
API_TOKEN=
# Local bridge IP; alternatively run `wow-hue discover`.
HUE_BRIDGE_IP=
